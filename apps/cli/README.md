# agent-context-lens

CLI package for [Agent Context Lens](../../README.md).

```bash
npx agent-context-lens serve . --file src/index.ts
npx agent-context-lens inspect . --file src/index.ts --agent all
```

The tool is local, read-only, deterministic by default, and requires Node.js 20 or later.
